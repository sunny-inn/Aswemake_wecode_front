import styled from 'styled-components';
import {
  Container as MapDiv,
  NaverMap,
  Marker,
  useNavermaps,
} from 'react-naver-maps';

export const MapBox = styled(MapDiv)`
  height: 640px;
  width: 360px;
`;
